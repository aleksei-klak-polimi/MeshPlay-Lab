description: Authentication is required or the provided credentials are invalid.
content:
  application/json:
    schema:
      allOf:
        - $ref: '../../schemas/common.yaml#/ErrorResponse'
        - type: object
          properties:
            error:
              type: object
              properties:
                code:
                  type: string
                  enum:
                    - MISSING_AUTH_HEADER
                    - INVALID_JWT
    examples:
      missingAuthHeader:
        summary: Missing Authorization header
        value:
          success: false
          message: "Missing Authorization header"
          error:
            code: MISSING_AUTH_HEADER
          meta:
            requestId: "string"
            timestamp: "2025-11-17T15:23:39.890Z"
      invalidJwt:
        summary: Invalid JWT
        value:
          success: false
          message: "The provided JWT token is invalid or expired"
          error:
            code: INVALID_JWT
          meta:
            requestId: "string"
            timestamp: "2025-11-17T15:23:39.890Z"
